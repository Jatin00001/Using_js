// let a = prompt("Enter Your age ?"); // by default this input is in string from so we have to change in integer.;

let a  = "44";
a = Number.parseInt(a); //this will convert string into int only if possible
console.log(a);
console.log(typeof(a));